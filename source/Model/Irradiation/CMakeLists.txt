# ----------------------------------------------------------
# Define header dir and includes 
# ----------------------------------------------------------

# include directories 
SET( CLASS_HEADERS_DIR_IRRADIATION ${CMAKE_CURRENT_SOURCE_DIR}  CACHE INTERNAL "")

LIST( APPEND CLASS_SRC_FILE_IRRADIATION "${CMAKE_CURRENT_SOURCE_DIR}/IM_Matrix.cxx")
LIST( APPEND CLASS_SRC_FILE_IRRADIATION "${CMAKE_CURRENT_SOURCE_DIR}/IM_RK4.cxx")

SET( CLASS_SRC_FILE_IRRADIATION ${CLASS_SRC_FILE_IRRADIATION} PARENT_SCOPE)

message("------- CLASS_SRC_FILE VARIABLE -------")
MESSAGE( STATUS "CLASS_SRC_FILE_IRRADIATION=" ${CLASS_SRC_FILE_IRRADIATION})
